apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: ecommerce-lab
  labels:
    app.kubernetes.io/name: order-service
spec:
  selector:
    app.kubernetes.io/name: order-service
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
