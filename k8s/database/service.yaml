apiVersion: v1
kind: Service
metadata:
  name: mariadb
  namespace: ecommerce-lab
spec:
  selector:
    app.kubernetes.io/name: mariadb
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
  type: ClusterIP
