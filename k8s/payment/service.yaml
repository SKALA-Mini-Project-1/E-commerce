apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: ecommerce-lab
  labels:
    app.kubernetes.io/name: payment-service
spec:
  selector:
    app.kubernetes.io/name: payment-service
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
