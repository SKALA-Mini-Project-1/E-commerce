apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: ecommerce-lab
  labels:
    app.kubernetes.io/name: product-service
spec:
  selector:
    app.kubernetes.io/name: product-service
  ports:
    - name: http
      port: 8080
      targetPort: http
  type: ClusterIP
